/**
 * Prettier settings file.
 *
 * Prettier provides automatic source code formatting for the project (JavaScript, TypeScript, CSS,
 * etc). The default settings are pretty good but here we can overwrite them.
 *
 * See:
 *  + https://prettier.io/
 *  + https://prettier.io/docs/en/configuration.html
 *  + https://prettier.io/docs/en/options.html - list of options and their default values
 */
module.exports = {
  "printWidth": 100,
  "trailingComma": "es5",
};